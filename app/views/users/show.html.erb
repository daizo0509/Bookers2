<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="container">
    <nav class="nav navbar-nav navbar-left">
      <span>Bookers</span>
    </nav>
    <nav>
      <ul class="nav navbar-nav navbar-right">
          <li class="">
          	<%= link_to "home",user_path(current_user.id) %>
          </li>
          <li class="">
         	<%= link_to "Users",users_path %>
          </li>
          <li class="">
            <%= link_to "books",books_path %>
          </li>
          <li class="">
            <%= link_to "logout",destroy_user_session_path, method: :delete %>
          </li>
      </ul>
    </nav>
  </div>
</header>
<h1>マイページ</h1>
<div class="col-xs-3">
    <h2>User info</h2>
    <% @user each do |book|%>
    <img class="attachment user profile_image fallback" src="/assets/no_image-ede28af4bad96368ef41be1b4be1cffce0d9402540f75d17895e32cb0d2af5e6.jpg" alt="No image"><table class="table">
      <tbody><tr></tr> 
      <tr>
        <th>name</th>
        <th><%=></th>
      </tr>
      <tr>
        <th>introduction</th>
        <th></th>
      </tr>
    </tbody></table>
    <div class="row">
      <a class="col-xs-12 btn btn-default glyphicon glyphicon-wrench" href="/users/122/edit"></a>
    </div>

    
  </div>

<table>
  <thead>
    <tr>
        <th>Title</th>
        <th>Body</th>
     </tr>
    </thead>
    <tbody>
      <% @books.each do |book| %>
      <% if book.errors.any? %>
      <% end %>
      <tr>
        <td class="title"><%= book.title %></td>
        <td class="body"><%= book.body %></td>
        <td>

          <%= link_to"Show", book_path(book.id) %>
          <%= link_to"Edit", edit_book_path(book.id) %>
          <%= link_to"Destroy", book_path(book.id), method: :delete, "data-confirm" =>"Are you sure?" %>
        </td>
      </tr>
      <% end %>
    </tbody>
</table> 
